name: Visual Regression (placeholder)

on:
  pull_request:
    branches: [ main ]

jobs:
  visual:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install
        run: bun install --frozen-lockfile

      - name: Build
        run: bun run build

      - name: Visual test (placeholder)
        run: |
          echo "Configure Playwright/Chromatic/ Percy here to take screenshots of component docs and compare against baseline."
